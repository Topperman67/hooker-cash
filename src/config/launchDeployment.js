// Deliberately unset until the deployment/treasury choice and current source
// review are resolved. Never import the original reference manifest here by default.
// See docs/LAUNCH_INTEGRATION.md for the required deployment contract.
export const launchDeployment = null
