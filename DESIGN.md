# Hookbrew — midnight liquid glass

## Home and navigation

The home screen keeps the original midnight (#090411), plum (#12081f and #1c0c30), porcelain (#f6f3ff), and lavender (#b4a1ff) palette. Keep the large Outfit headline beside three transparent QuickLiquid layers representing swaps, the hook, and the Uniswap V4 pool. Pointer movement tilts the layers and shifts their highlights; hover lifts the stack apart. Use CSS 3D transforms, update pointer values at most once per frame, and disable this decorative movement for reduced-motion or touch input. The supporting market section displays actual recent launches, distinct loading and empty states, and an explicit retry when data is unavailable. Never fill the space with invented market statistics.

Desktop navigation starts as a narrow glass rail containing the flask logo and all nine platinum section icons. Icons link directly to their routes and show their names on hover or keyboard focus. Clicking the logo or expansion arrow opens the full labeled sidebar; clicking the expanded logo or close button collapses it back to the rail. The content resizes beside the sidebar. On mobile the logo opens a modal drawer with a backdrop, focus containment, Escape dismissal, and focus restoration. Hidden navigation is inert. Both desktop rail and expanded navigation fit short windows without an inner scrollbar.

## Launch studio and terminal

Keep midnight #090411, plum #12081f, porcelain #f6f3ff, lavender #b4a1ff, mint #a5e7d2 and ice #b9edff. Outfit carries headings and token identity; DM Sans carries controls and data. The creator workspace is one centered glass surface with the sequence Pool → Hook → Token → Payouts → Review. Keep the complete step indicator visible on mobile. Pool uses valuation and fee sliders; Hook uses selectable cards; Token contains identity and artwork; Payouts contains the founder buy, vesting presets and wallet splits. Review groups the exact values with edit links before simulation and wallet signing. The terminal makes the price chart the dominant surface, with a fixed-width trade ticket and a tabular activity/rewards area below. Align inputs, prices and transaction amounts consistently; reserve mint/pink for trade direction and lavender for primary actions.

The specific identity is a brewing workbench followed by a trading desk: uploaded token art, its symbol and its configured fee distribution connect both. Do not turn the launch form into a stack of identical promotional cards or put decorative sample candles into the trading chart. Empty market data remains empty. Draft previews are labeled as previews. Dense terminal controls still work at mobile widths.

Use an aubergine-black background (#090411), dark plum surfaces (#12081f and #1c0c30), violet edges (#b4a1ff), mint (#a5e7d2), and porcelain type (#f6f3ff). Broad surfaces stay dark. Outfit headings, DM Sans controls, and the original flask-and-hook wordmark establish the identity.

The left navigation uses nine original transparent PNGs from `public/brand/navigation/platinum`: dashboard tiles for overview, candlesticks for market, a plus coin for creation, opposing arrows for trade, paired drops for liquidity, a trophy for rankings, a puzzle piece for the hook registry, a robot head for agents, and an open book for documentation. Their satin platinum glass has restrained sage reflections, substantial silhouettes, and no neon glow. Use midnight #090411, dark plum #12081f, readable neutral #b9b5c3, porcelain #f0edf4, and restrained sage #b9cec8 for the active item. Keep DM Sans labels left aligned beside consistent 36px artwork boxes, reduced on short windows. Outfit and the glass flask remain the brand signature. The sidebar keeps its liquid-glass surface while the navigation stays quiet and legible.

Use one continuous navigation list with 46px rows and 8px spacing, followed by a quiet Arc network card and help at the bottom. The card uses a blue Arc mark, a clear network heading, and a larger latest-block value above the explorer link. Connection state and block values come from the actual RPC. Keep the flask-and-hook logo silhouette, with a restrained cool-blue tint and a soft edge highlight; retain porcelain lettering and platinum navigation icons. Blue #82b2f4 is confined to the brand and network accents, against the midnight/plum surfaces. Compact the card below 881px height, compact the brand and rows on short windows, hide the supplementary network card below 821px height, and use two columns in the mobile landscape drawer so every destination remains visible without an inner scrollbar. Retain visible focus, mobile close controls, and accessible link labels.

QuickLiquid refraction is always configured on. No user-facing off/lightweight mode exists, and the obsolete preference is ignored. Retain the engine's system accessibility preferences and browser fallbacks. Destroy lenses and listeners on unmount.

Real data has priority over a visually busy dashboard. Every token identity must come from a contract or a verified metadata source. No invented markets, prices, trades, balances, liquidity locks, quotes, transaction receipts, or generated chart histories. Use readable unavailable states until a real source is connected. Architecture illustrations must be identified as illustrations.

Current implementation connects browser wallets, Arc RPC, and ERC-20 metadata lookup. Launching, swapping, pool indexing, rankings, and agent access remain incomplete; they must not be presented as working features.
